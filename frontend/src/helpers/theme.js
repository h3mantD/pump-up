import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    white: {
      main: "#ffffff",
      light: "#ffffff",
      dark: "#ffffff",
      contrastText: "#ffffff"
    }
  }
});

export default theme;
